# Exemplos/relatórios reais usados só como referência local — nunca publicar
# (contêm nome real de reclamante/reclamado, número de processo real).
PROCESSO_*
RELATORIO_PROCESSO_*
*.PJC

# Prints originais enviados pelo usuário — as versões organizadas ficam em imagens/
IMAGEM/
